# Git Practice – MLPro1

This repository is created to practice basic **Git commands** and understand how Git tracks files and commits.

---

## Git Commands Used

```bash
git init
git status
git ls-files
git add
git commit
git log
git show
git rm
git rm --cached


---

## Git Commands Practiced

### Initialize repository
```bash
git init

git status
git ls-files

git add <file>

git commit -m "message"

git log
git log --oneline

git show

git rm --cached <file>
git rm -f <file>
